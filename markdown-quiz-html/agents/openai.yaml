interface:
  display_name: "Markdown Quiz HTML (Legacy)"
  short_description: "Legacy markdown quiz entry point"
  default_prompt: "Use $markdown-quiz-html only when you need the legacy markdown quiz entry point; otherwise prefer $offline-quiz-html."

policy:
  allow_implicit_invocation: true
